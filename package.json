{
  "name": "living-fix-note-autopublish",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub PR approval to Blogger automatic publishing",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-posts.js",
    "publish": "node scripts/publish-changed.js",
    "oauth": "node scripts/get-refresh-token.js"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
